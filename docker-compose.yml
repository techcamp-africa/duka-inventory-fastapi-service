version: '3'
services: 
    api:
        build: .
        command: uvicorn main:app --reload --workers 1 --host 0.0.0.0  --port 8006
        container_name: sportsgeek-api
        restart: always
        ports: 
            - 8006:80
        